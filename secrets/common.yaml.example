# Encrypt this file with sops after adding real recipients in .sops.yaml.
# Shared secret structure example:
#
# github:
#   token: ghp_example
#
# cloudflare:
#   apiToken: example
#
# cortecs:
#   apiKey: your-cortecs-api-key
#
# Current wired secrets:
# - github.token    -> /run/secrets/github-token
# - cortecs.apiKey  -> /run/secrets/cortecs-api-key  (injected into ~/.pi/agent/llm-router.json)
