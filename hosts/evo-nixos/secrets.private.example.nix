{
  # Copy this file to secrets.private.nix and fill in your API keys.
  # This file is gitignored — never commit secrets.
  #
  # After editing, rebuild: sudo nixos-rebuild switch --flake ~/Workspace/NixPI#evo-nixos

  secrets.syntheticApiKey = "sk-your-synthetic-api-key-here";
}
